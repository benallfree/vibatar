export { load } from './Vibatar.js'

// Example usage:
/*
import { load } from 'vibatar'

// Load a player's avatar
const avatar = await load('player-name', {
  optimization: {
    maxPolygons: 10000,
    useLOD: true
  }
})

// Add the avatar to your scene
scene.add(avatar.model)

// Clean up when done
avatar.dispose()
*/
